[project]
name = "tastycrousty"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Omrane", email = "omraneriahi@hotmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13.5",
    "uvicorn>=0.52.4",
]

[project.scripts]
tastycrousty = "tastycrousty:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
